# Generated / runtime data - not versioned (regenerated by the app)
data/live_prices.csv
data/screen_status.txt
data/export_screen.csv
*.tmp

# Analyze-tab per-ticker scratch (rewritten on every "Analyze" run)
data/detail_summary.csv
data/detail_history.csv
data/detail_analysts.csv
data/detail_earnings.csv
data/detail_status.txt
data/detail_error.log

# Distribution packages are attached on release, not committed
*.zip

# The screen results and user state ARE worth keeping in the shared
# package so it works out of the box, but in a dev repo you may want
# to ignore them. Uncomment if so:
# data/screen_data.csv
# data/screen_meta.txt
# data/trades.csv
# data/pinned.csv

# Error logs
error_log.txt

# OS noise
.DS_Store
Thumbs.db
desktop.ini

# Python
__pycache__/
*.pyc
